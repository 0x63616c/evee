#!/bin/bash
# Run database migrations using the new image before container swap
VERSION=$KAMAL_VERSION kamal app exec -p -c config/deploy.api.yml \
  "bunx drizzle-kit migrate && bun run src/db/seed.ts"
